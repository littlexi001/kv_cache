#!/usr/bin/env bash
set -e

cd "$(dirname "$0")"

export RUN_NAME="${RUN_NAME:-inverse-kv-supervised-gate-zipf-high-hash}"
export CKPT_DIR="${CKPT_DIR:-../checkpoints/${RUN_NAME}}"

export SYNTHETIC_SAMPLING_DISTRIBUTION="${SYNTHETIC_SAMPLING_DISTRIBUTION:-zipf}"
export GROUND_TRUTH_ROUTING_STRATEGY="${GROUND_TRUTH_ROUTING_STRATEGY:-hash}"
export GROUND_TRUTH_ROUTING_FEATURE_LAYER="${GROUND_TRUTH_ROUTING_FEATURE_LAYER:-1}"
export GROUND_TRUTH_ROUTING_MODE="${GROUND_TRUTH_ROUTING_MODE:-supervise}"
export MOE_ROUTER_SUPERVISION_LOSS_WEIGHT="${MOE_ROUTER_SUPERVISION_LOSS_WEIGHT:-1.0}"
export MOE_ROUTER_SUPERVISION_DETACH_INPUT="${MOE_ROUTER_SUPERVISION_DETACH_INPUT:-false}"

export USE_MOE="${USE_MOE:-true}"
export MOE_NUM_UNIQUE_EXPERTS="${MOE_NUM_UNIQUE_EXPERTS:-4}"
export MOE_NUM_EXPERTS_PER_TOK="${MOE_NUM_EXPERTS_PER_TOK:-1}"
export MOE_HEAD_LEVEL="${MOE_HEAD_LEVEL:-false}"
export MOE_ROUTER_INPUT="${MOE_ROUTER_INPUT:-hidden}"
export MOE_LOAD_BALANCE_LOSS_WEIGHT="${MOE_LOAD_BALANCE_LOSS_WEIGHT:-0.0}"

export TOTAL_TRAINING_STEPS="${TOTAL_TRAINING_STEPS:-5000}"
export SAVE_INTERVAL="${SAVE_INTERVAL:-1000}"

bash run_inverse_kv_local_experiment.sh
