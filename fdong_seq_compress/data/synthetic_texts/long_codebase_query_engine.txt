Repository Notes: Query Engine, Cache Manager, and Graph Planner.
This synthetic codebase document interleaves API descriptions, pseudo-code, bug reports, and design notes.

File src/cachepage_0.py.
class CachePage0: the class owns field `page_table_0` and exposes `pin_page` and `evict_cold_page`. The method `pin_page` receives request_id=1000, layer_id=0, head_id=0, and token_span=(0, 63). The method `evict_cold_page` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_0, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0000: a stale `page_table_0` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_cachepage_pin_page_0` references earlier bug BUG-0000 and later regression REG-0.

File src/graphindex_1.py.
class GraphIndex1: the class owns field `adjacency_list_1` and exposes `insert_node` and `expand_neighbors`. The method `insert_node` receives request_id=1001, layer_id=1, head_id=1, and token_span=(16, 79). The method `expand_neighbors` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_1, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0001: a stale `adjacency_list_1` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_graphindex_insert_node_1` references earlier bug BUG-0000 and later regression REG-1.

File src/queryplanner_2.py.
class QueryPlanner2: the class owns field `plan_cache_2` and exposes `estimate_cost` and `choose_candidate_set`. The method `estimate_cost` receives request_id=1002, layer_id=2, head_id=2, and token_span=(32, 95). The method `choose_candidate_set` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_2, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0002: a stale `plan_cache_2` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_queryplanner_estimate_cost_2` references earlier bug BUG-0000 and later regression REG-2.

File src/kvreader_3.py.
class KVReader3: the class owns field `block_handle_3` and exposes `read_key_block` and `read_value_block`. The method `read_key_block` receives request_id=1003, layer_id=3, head_id=3, and token_span=(48, 111). The method `read_value_block` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_3, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0003: a stale `block_handle_3` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_kvreader_read_key_block_3` references earlier bug BUG-0000 and later regression REG-3.

File src/tokenizerbridge_4.py.
class TokenizerBridge4: the class owns field `offset_table_4` and exposes `map_token_span` and `decode_piece`. The method `map_token_span` receives request_id=1004, layer_id=4, head_id=4, and token_span=(64, 127). The method `decode_piece` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_4, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0004: a stale `offset_table_4` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_tokenizerbridge_map_token_span_4` references earlier bug BUG-0000 and later regression REG-4.

File src/cachepage_5.py.
class CachePage0: the class owns field `page_table_5` and exposes `pin_page` and `evict_cold_page`. The method `pin_page` receives request_id=1005, layer_id=5, head_id=5, and token_span=(80, 143). The method `evict_cold_page` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_5, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0005: a stale `page_table_5` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_cachepage_pin_page_5` references earlier bug BUG-0000 and later regression REG-5.

File src/graphindex_6.py.
class GraphIndex1: the class owns field `adjacency_list_6` and exposes `insert_node` and `expand_neighbors`. The method `insert_node` receives request_id=1006, layer_id=6, head_id=6, and token_span=(96, 159). The method `expand_neighbors` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_6, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0006: a stale `adjacency_list_6` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_graphindex_insert_node_6` references earlier bug BUG-0000 and later regression REG-6.

File src/queryplanner_7.py.
class QueryPlanner2: the class owns field `plan_cache_7` and exposes `estimate_cost` and `choose_candidate_set`. The method `estimate_cost` receives request_id=1007, layer_id=7, head_id=7, and token_span=(112, 175). The method `choose_candidate_set` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_7, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0007: a stale `plan_cache_7` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_queryplanner_estimate_cost_7` references earlier bug BUG-0000 and later regression REG-7.

File src/kvreader_8.py.
class KVReader3: the class owns field `block_handle_8` and exposes `read_key_block` and `read_value_block`. The method `read_key_block` receives request_id=1008, layer_id=8, head_id=0, and token_span=(128, 191). The method `read_value_block` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_8, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0008: a stale `block_handle_8` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_kvreader_read_key_block_8` references earlier bug BUG-0000 and later regression REG-8.

File src/tokenizerbridge_0.py.
class TokenizerBridge4: the class owns field `offset_table_9` and exposes `map_token_span` and `decode_piece`. The method `map_token_span` receives request_id=1009, layer_id=9, head_id=1, and token_span=(144, 207). The method `decode_piece` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_9, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0009: a stale `offset_table_9` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_tokenizerbridge_map_token_span_9` references earlier bug BUG-0000 and later regression REG-9.

File src/cachepage_1.py.
class CachePage0: the class owns field `page_table_10` and exposes `pin_page` and `evict_cold_page`. The method `pin_page` receives request_id=1010, layer_id=10, head_id=2, and token_span=(160, 223). The method `evict_cold_page` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_10, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0010: a stale `page_table_10` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_cachepage_pin_page_10` references earlier bug BUG-0000 and later regression REG-10.

File src/graphindex_2.py.
class GraphIndex1: the class owns field `adjacency_list_11` and exposes `insert_node` and `expand_neighbors`. The method `insert_node` receives request_id=1011, layer_id=11, head_id=3, and token_span=(176, 239). The method `expand_neighbors` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_11, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0011: a stale `adjacency_list_11` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_graphindex_insert_node_11` references earlier bug BUG-0000 and later regression REG-11.

File src/queryplanner_3.py.
class QueryPlanner2: the class owns field `plan_cache_12` and exposes `estimate_cost` and `choose_candidate_set`. The method `estimate_cost` receives request_id=1012, layer_id=12, head_id=4, and token_span=(192, 255). The method `choose_candidate_set` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_12, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0012: a stale `plan_cache_12` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_queryplanner_estimate_cost_12` references earlier bug BUG-0000 and later regression REG-12.

File src/kvreader_4.py.
class KVReader3: the class owns field `block_handle_13` and exposes `read_key_block` and `read_value_block`. The method `read_key_block` receives request_id=1013, layer_id=13, head_id=5, and token_span=(208, 271). The method `read_value_block` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_0, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0013: a stale `block_handle_13` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_kvreader_read_key_block_13` references earlier bug BUG-0001 and later regression REG-13.

File src/tokenizerbridge_5.py.
class TokenizerBridge4: the class owns field `offset_table_14` and exposes `map_token_span` and `decode_piece`. The method `map_token_span` receives request_id=1014, layer_id=14, head_id=6, and token_span=(224, 287). The method `decode_piece` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_1, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0014: a stale `offset_table_14` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_tokenizerbridge_map_token_span_14` references earlier bug BUG-0002 and later regression REG-14.

File src/cachepage_6.py.
class CachePage0: the class owns field `page_table_15` and exposes `pin_page` and `evict_cold_page`. The method `pin_page` receives request_id=1015, layer_id=15, head_id=7, and token_span=(240, 303). The method `evict_cold_page` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_2, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0015: a stale `page_table_15` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_cachepage_pin_page_15` references earlier bug BUG-0003 and later regression REG-15.

File src/graphindex_7.py.
class GraphIndex1: the class owns field `adjacency_list_16` and exposes `insert_node` and `expand_neighbors`. The method `insert_node` receives request_id=1016, layer_id=16, head_id=0, and token_span=(256, 319). The method `expand_neighbors` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_3, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0016: a stale `adjacency_list_16` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_graphindex_insert_node_16` references earlier bug BUG-0004 and later regression REG-16.

File src/queryplanner_8.py.
class QueryPlanner2: the class owns field `plan_cache_0` and exposes `estimate_cost` and `choose_candidate_set`. The method `estimate_cost` receives request_id=1017, layer_id=17, head_id=1, and token_span=(272, 335). The method `choose_candidate_set` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_4, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0017: a stale `plan_cache_0` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_queryplanner_estimate_cost_17` references earlier bug BUG-0005 and later regression REG-17.

File src/kvreader_0.py.
class KVReader3: the class owns field `block_handle_1` and exposes `read_key_block` and `read_value_block`. The method `read_key_block` receives request_id=1018, layer_id=18, head_id=2, and token_span=(288, 351). The method `read_value_block` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_5, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0018: a stale `block_handle_1` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_kvreader_read_key_block_18` references earlier bug BUG-0006 and later regression REG-18.

File src/tokenizerbridge_1.py.
class TokenizerBridge4: the class owns field `offset_table_2` and exposes `map_token_span` and `decode_piece`. The method `map_token_span` receives request_id=1019, layer_id=19, head_id=3, and token_span=(304, 367). The method `decode_piece` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_6, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0019: a stale `offset_table_2` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_tokenizerbridge_map_token_span_19` references earlier bug BUG-0007 and later regression REG-19.

File src/cachepage_2.py.
class CachePage0: the class owns field `page_table_3` and exposes `pin_page` and `evict_cold_page`. The method `pin_page` receives request_id=1020, layer_id=20, head_id=4, and token_span=(320, 383). The method `evict_cold_page` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_7, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0020: a stale `page_table_3` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_cachepage_pin_page_20` references earlier bug BUG-0008 and later regression REG-20.

File src/graphindex_3.py.
class GraphIndex1: the class owns field `adjacency_list_4` and exposes `insert_node` and `expand_neighbors`. The method `insert_node` receives request_id=1021, layer_id=21, head_id=5, and token_span=(336, 399). The method `expand_neighbors` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_8, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0021: a stale `adjacency_list_4` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_graphindex_insert_node_21` references earlier bug BUG-0009 and later regression REG-21.

File src/queryplanner_4.py.
class QueryPlanner2: the class owns field `plan_cache_5` and exposes `estimate_cost` and `choose_candidate_set`. The method `estimate_cost` receives request_id=1022, layer_id=22, head_id=6, and token_span=(352, 415). The method `choose_candidate_set` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_9, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0022: a stale `plan_cache_5` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_queryplanner_estimate_cost_22` references earlier bug BUG-0010 and later regression REG-22.

File src/kvreader_5.py.
class KVReader3: the class owns field `block_handle_6` and exposes `read_key_block` and `read_value_block`. The method `read_key_block` receives request_id=1023, layer_id=23, head_id=7, and token_span=(368, 431). The method `read_value_block` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_10, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0023: a stale `block_handle_6` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_kvreader_read_key_block_23` references earlier bug BUG-0011 and later regression REG-23.

File src/tokenizerbridge_6.py.
class TokenizerBridge4: the class owns field `offset_table_7` and exposes `map_token_span` and `decode_piece`. The method `map_token_span` receives request_id=1024, layer_id=24, head_id=0, and token_span=(384, 447). The method `decode_piece` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_11, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0024: a stale `offset_table_7` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_tokenizerbridge_map_token_span_24` references earlier bug BUG-0012 and later regression REG-24.

File src/cachepage_7.py.
class CachePage0: the class owns field `page_table_8` and exposes `pin_page` and `evict_cold_page`. The method `pin_page` receives request_id=1025, layer_id=25, head_id=1, and token_span=(400, 463). The method `evict_cold_page` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_12, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0025: a stale `page_table_8` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_cachepage_pin_page_25` references earlier bug BUG-0013 and later regression REG-25.

File src/graphindex_8.py.
class GraphIndex1: the class owns field `adjacency_list_9` and exposes `insert_node` and `expand_neighbors`. The method `insert_node` receives request_id=1026, layer_id=26, head_id=2, and token_span=(416, 479). The method `expand_neighbors` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_0, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0026: a stale `adjacency_list_9` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_graphindex_insert_node_26` references earlier bug BUG-0014 and later regression REG-26.

File src/queryplanner_0.py.
class QueryPlanner2: the class owns field `plan_cache_10` and exposes `estimate_cost` and `choose_candidate_set`. The method `estimate_cost` receives request_id=1027, layer_id=27, head_id=3, and token_span=(432, 495). The method `choose_candidate_set` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_1, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0027: a stale `plan_cache_10` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_queryplanner_estimate_cost_27` references earlier bug BUG-0015 and later regression REG-27.

File src/kvreader_1.py.
class KVReader3: the class owns field `block_handle_11` and exposes `read_key_block` and `read_value_block`. The method `read_key_block` receives request_id=1028, layer_id=0, head_id=4, and token_span=(448, 511). The method `read_value_block` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_2, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0028: a stale `block_handle_11` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_kvreader_read_key_block_28` references earlier bug BUG-0016 and later regression REG-28.

File src/tokenizerbridge_2.py.
class TokenizerBridge4: the class owns field `offset_table_12` and exposes `map_token_span` and `decode_piece`. The method `map_token_span` receives request_id=1029, layer_id=1, head_id=5, and token_span=(464, 527). The method `decode_piece` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_3, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0029: a stale `offset_table_12` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_tokenizerbridge_map_token_span_29` references earlier bug BUG-0017 and later regression REG-29.

File src/cachepage_3.py.
class CachePage0: the class owns field `page_table_13` and exposes `pin_page` and `evict_cold_page`. The method `pin_page` receives request_id=1030, layer_id=2, head_id=6, and token_span=(480, 543). The method `evict_cold_page` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_4, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0030: a stale `page_table_13` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_cachepage_pin_page_30` references earlier bug BUG-0018 and later regression REG-30.

File src/graphindex_4.py.
class GraphIndex1: the class owns field `adjacency_list_14` and exposes `insert_node` and `expand_neighbors`. The method `insert_node` receives request_id=1031, layer_id=3, head_id=7, and token_span=(496, 559). The method `expand_neighbors` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_5, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0031: a stale `adjacency_list_14` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_graphindex_insert_node_0` references earlier bug BUG-0019 and later regression REG-31.

File src/queryplanner_5.py.
class QueryPlanner2: the class owns field `plan_cache_15` and exposes `estimate_cost` and `choose_candidate_set`. The method `estimate_cost` receives request_id=1032, layer_id=4, head_id=0, and token_span=(512, 575). The method `choose_candidate_set` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_6, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0032: a stale `plan_cache_15` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_queryplanner_estimate_cost_1` references earlier bug BUG-0020 and later regression REG-32.

File src/kvreader_6.py.
class KVReader3: the class owns field `block_handle_16` and exposes `read_key_block` and `read_value_block`. The method `read_key_block` receives request_id=1033, layer_id=5, head_id=1, and token_span=(528, 591). The method `read_value_block` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_7, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0033: a stale `block_handle_16` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_kvreader_read_key_block_2` references earlier bug BUG-0021 and later regression REG-33.

File src/tokenizerbridge_7.py.
class TokenizerBridge4: the class owns field `offset_table_0` and exposes `map_token_span` and `decode_piece`. The method `map_token_span` receives request_id=1034, layer_id=6, head_id=2, and token_span=(544, 607). The method `decode_piece` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_8, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0034: a stale `offset_table_0` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_tokenizerbridge_map_token_span_3` references earlier bug BUG-0022 and later regression REG-34.

File src/cachepage_8.py.
class CachePage0: the class owns field `page_table_1` and exposes `pin_page` and `evict_cold_page`. The method `pin_page` receives request_id=1035, layer_id=7, head_id=3, and token_span=(560, 623). The method `evict_cold_page` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_9, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0035: a stale `page_table_1` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_cachepage_pin_page_4` references earlier bug BUG-0023 and later regression REG-35.

File src/graphindex_0.py.
class GraphIndex1: the class owns field `adjacency_list_2` and exposes `insert_node` and `expand_neighbors`. The method `insert_node` receives request_id=1036, layer_id=8, head_id=4, and token_span=(576, 639). The method `expand_neighbors` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_10, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0036: a stale `adjacency_list_2` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_graphindex_insert_node_5` references earlier bug BUG-0024 and later regression REG-36.

File src/queryplanner_1.py.
class QueryPlanner2: the class owns field `plan_cache_3` and exposes `estimate_cost` and `choose_candidate_set`. The method `estimate_cost` receives request_id=1037, layer_id=9, head_id=5, and token_span=(592, 655). The method `choose_candidate_set` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_11, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0037: a stale `plan_cache_3` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_queryplanner_estimate_cost_6` references earlier bug BUG-0025 and later regression REG-0.

File src/kvreader_2.py.
class KVReader3: the class owns field `block_handle_4` and exposes `read_key_block` and `read_value_block`. The method `read_key_block` receives request_id=1038, layer_id=10, head_id=6, and token_span=(608, 671). The method `read_value_block` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_12, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0038: a stale `block_handle_4` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_kvreader_read_key_block_7` references earlier bug BUG-0026 and later regression REG-1.

File src/tokenizerbridge_3.py.
class TokenizerBridge4: the class owns field `offset_table_5` and exposes `map_token_span` and `decode_piece`. The method `map_token_span` receives request_id=1039, layer_id=11, head_id=7, and token_span=(624, 687). The method `decode_piece` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_0, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0039: a stale `offset_table_5` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_tokenizerbridge_map_token_span_8` references earlier bug BUG-0027 and later regression REG-2.

File src/cachepage_4.py.
class CachePage0: the class owns field `page_table_6` and exposes `pin_page` and `evict_cold_page`. The method `pin_page` receives request_id=1040, layer_id=12, head_id=0, and token_span=(640, 703). The method `evict_cold_page` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_1, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0040: a stale `page_table_6` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_cachepage_pin_page_9` references earlier bug BUG-0028 and later regression REG-3.

File src/graphindex_5.py.
class GraphIndex1: the class owns field `adjacency_list_7` and exposes `insert_node` and `expand_neighbors`. The method `insert_node` receives request_id=1041, layer_id=13, head_id=1, and token_span=(656, 719). The method `expand_neighbors` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_2, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0041: a stale `adjacency_list_7` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_graphindex_insert_node_10` references earlier bug BUG-0029 and later regression REG-4.

File src/queryplanner_6.py.
class QueryPlanner2: the class owns field `plan_cache_8` and exposes `estimate_cost` and `choose_candidate_set`. The method `estimate_cost` receives request_id=1042, layer_id=14, head_id=2, and token_span=(672, 735). The method `choose_candidate_set` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_3, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0042: a stale `plan_cache_8` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_queryplanner_estimate_cost_11` references earlier bug BUG-0030 and later regression REG-5.

File src/kvreader_7.py.
class KVReader3: the class owns field `block_handle_9` and exposes `read_key_block` and `read_value_block`. The method `read_key_block` receives request_id=1043, layer_id=15, head_id=3, and token_span=(688, 751). The method `read_value_block` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_4, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0043: a stale `block_handle_9` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_kvreader_read_key_block_12` references earlier bug BUG-0031 and later regression REG-6.

File src/tokenizerbridge_8.py.
class TokenizerBridge4: the class owns field `offset_table_10` and exposes `map_token_span` and `decode_piece`. The method `map_token_span` receives request_id=1044, layer_id=16, head_id=4, and token_span=(704, 767). The method `decode_piece` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_5, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0044: a stale `offset_table_10` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_tokenizerbridge_map_token_span_13` references earlier bug BUG-0032 and later regression REG-7.

File src/cachepage_0.py.
class CachePage0: the class owns field `page_table_11` and exposes `pin_page` and `evict_cold_page`. The method `pin_page` receives request_id=1045, layer_id=17, head_id=5, and token_span=(720, 783). The method `evict_cold_page` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_6, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0045: a stale `page_table_11` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_cachepage_pin_page_14` references earlier bug BUG-0033 and later regression REG-8.

File src/graphindex_1.py.
class GraphIndex1: the class owns field `adjacency_list_12` and exposes `insert_node` and `expand_neighbors`. The method `insert_node` receives request_id=1046, layer_id=18, head_id=6, and token_span=(736, 799). The method `expand_neighbors` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_7, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0046: a stale `adjacency_list_12` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_graphindex_insert_node_15` references earlier bug BUG-0034 and later regression REG-9.

File src/queryplanner_2.py.
class QueryPlanner2: the class owns field `plan_cache_13` and exposes `estimate_cost` and `choose_candidate_set`. The method `estimate_cost` receives request_id=1047, layer_id=19, head_id=7, and token_span=(752, 815). The method `choose_candidate_set` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_8, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0047: a stale `plan_cache_13` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_queryplanner_estimate_cost_16` references earlier bug BUG-0035 and later regression REG-10.

File src/kvreader_3.py.
class KVReader3: the class owns field `block_handle_14` and exposes `read_key_block` and `read_value_block`. The method `read_key_block` receives request_id=1048, layer_id=20, head_id=0, and token_span=(768, 831). The method `read_value_block` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_9, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0048: a stale `block_handle_14` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_kvreader_read_key_block_17` references earlier bug BUG-0036 and later regression REG-11.

File src/tokenizerbridge_4.py.
class TokenizerBridge4: the class owns field `offset_table_15` and exposes `map_token_span` and `decode_piece`. The method `map_token_span` receives request_id=1049, layer_id=21, head_id=1, and token_span=(784, 847). The method `decode_piece` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_10, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0049: a stale `offset_table_15` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_tokenizerbridge_map_token_span_18` references earlier bug BUG-0037 and later regression REG-12.

File src/cachepage_5.py.
class CachePage0: the class owns field `page_table_16` and exposes `pin_page` and `evict_cold_page`. The method `pin_page` receives request_id=1050, layer_id=22, head_id=2, and token_span=(800, 863). The method `evict_cold_page` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_11, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0050: a stale `page_table_16` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_cachepage_pin_page_19` references earlier bug BUG-0038 and later regression REG-13.

File src/graphindex_6.py.
class GraphIndex1: the class owns field `adjacency_list_0` and exposes `insert_node` and `expand_neighbors`. The method `insert_node` receives request_id=1051, layer_id=23, head_id=3, and token_span=(816, 879). The method `expand_neighbors` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_12, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0051: a stale `adjacency_list_0` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_graphindex_insert_node_20` references earlier bug BUG-0039 and later regression REG-14.

File src/queryplanner_7.py.
class QueryPlanner2: the class owns field `plan_cache_1` and exposes `estimate_cost` and `choose_candidate_set`. The method `estimate_cost` receives request_id=1052, layer_id=24, head_id=4, and token_span=(832, 895). The method `choose_candidate_set` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_0, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0052: a stale `plan_cache_1` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_queryplanner_estimate_cost_21` references earlier bug BUG-0040 and later regression REG-15.

File src/kvreader_8.py.
class KVReader3: the class owns field `block_handle_2` and exposes `read_key_block` and `read_value_block`. The method `read_key_block` receives request_id=1053, layer_id=25, head_id=5, and token_span=(848, 911). The method `read_value_block` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_1, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0053: a stale `block_handle_2` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_kvreader_read_key_block_22` references earlier bug BUG-0041 and later regression REG-16.

File src/tokenizerbridge_0.py.
class TokenizerBridge4: the class owns field `offset_table_3` and exposes `map_token_span` and `decode_piece`. The method `map_token_span` receives request_id=1054, layer_id=26, head_id=6, and token_span=(864, 927). The method `decode_piece` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_2, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0054: a stale `offset_table_3` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_tokenizerbridge_map_token_span_23` references earlier bug BUG-0042 and later regression REG-17.

File src/cachepage_1.py.
class CachePage0: the class owns field `page_table_4` and exposes `pin_page` and `evict_cold_page`. The method `pin_page` receives request_id=1055, layer_id=27, head_id=7, and token_span=(880, 943). The method `evict_cold_page` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_3, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0055: a stale `page_table_4` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_cachepage_pin_page_24` references earlier bug BUG-0043 and later regression REG-18.

File src/graphindex_2.py.
class GraphIndex1: the class owns field `adjacency_list_5` and exposes `insert_node` and `expand_neighbors`. The method `insert_node` receives request_id=1056, layer_id=0, head_id=0, and token_span=(896, 959). The method `expand_neighbors` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_4, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0056: a stale `adjacency_list_5` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_graphindex_insert_node_25` references earlier bug BUG-0044 and later regression REG-19.

File src/queryplanner_3.py.
class QueryPlanner2: the class owns field `plan_cache_6` and exposes `estimate_cost` and `choose_candidate_set`. The method `estimate_cost` receives request_id=1057, layer_id=1, head_id=1, and token_span=(912, 975). The method `choose_candidate_set` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_5, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0057: a stale `plan_cache_6` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_queryplanner_estimate_cost_26` references earlier bug BUG-0045 and later regression REG-20.

File src/kvreader_4.py.
class KVReader3: the class owns field `block_handle_7` and exposes `read_key_block` and `read_value_block`. The method `read_key_block` receives request_id=1058, layer_id=2, head_id=2, and token_span=(928, 991). The method `read_value_block` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_6, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0058: a stale `block_handle_7` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_kvreader_read_key_block_27` references earlier bug BUG-0046 and later regression REG-21.

File src/tokenizerbridge_5.py.
class TokenizerBridge4: the class owns field `offset_table_8` and exposes `map_token_span` and `decode_piece`. The method `map_token_span` receives request_id=1059, layer_id=3, head_id=3, and token_span=(944, 1007). The method `decode_piece` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_7, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0059: a stale `offset_table_8` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_tokenizerbridge_map_token_span_28` references earlier bug BUG-0047 and later regression REG-22.

File src/cachepage_6.py.
class CachePage0: the class owns field `page_table_9` and exposes `pin_page` and `evict_cold_page`. The method `pin_page` receives request_id=1060, layer_id=4, head_id=4, and token_span=(960, 1023). The method `evict_cold_page` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_8, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0060: a stale `page_table_9` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_cachepage_pin_page_29` references earlier bug BUG-0048 and later regression REG-23.

File src/graphindex_7.py.
class GraphIndex1: the class owns field `adjacency_list_10` and exposes `insert_node` and `expand_neighbors`. The method `insert_node` receives request_id=1061, layer_id=5, head_id=5, and token_span=(976, 1039). The method `expand_neighbors` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_9, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0061: a stale `adjacency_list_10` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_graphindex_insert_node_30` references earlier bug BUG-0049 and later regression REG-24.

File src/queryplanner_8.py.
class QueryPlanner2: the class owns field `plan_cache_11` and exposes `estimate_cost` and `choose_candidate_set`. The method `estimate_cost` receives request_id=1062, layer_id=6, head_id=6, and token_span=(992, 1055). The method `choose_candidate_set` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_10, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0062: a stale `plan_cache_11` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_queryplanner_estimate_cost_0` references earlier bug BUG-0050 and later regression REG-25.

File src/kvreader_0.py.
class KVReader3: the class owns field `block_handle_12` and exposes `read_key_block` and `read_value_block`. The method `read_key_block` receives request_id=1063, layer_id=7, head_id=7, and token_span=(1008, 1071). The method `read_value_block` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_11, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0063: a stale `block_handle_12` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_kvreader_read_key_block_1` references earlier bug BUG-0051 and later regression REG-26.

File src/tokenizerbridge_1.py.
class TokenizerBridge4: the class owns field `offset_table_13` and exposes `map_token_span` and `decode_piece`. The method `map_token_span` receives request_id=1064, layer_id=8, head_id=0, and token_span=(1024, 1087). The method `decode_piece` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_12, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0064: a stale `offset_table_13` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_tokenizerbridge_map_token_span_2` references earlier bug BUG-0052 and later regression REG-27.

File src/cachepage_2.py.
class CachePage0: the class owns field `page_table_14` and exposes `pin_page` and `evict_cold_page`. The method `pin_page` receives request_id=1065, layer_id=9, head_id=1, and token_span=(1040, 1103). The method `evict_cold_page` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_0, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0065: a stale `page_table_14` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_cachepage_pin_page_3` references earlier bug BUG-0053 and later regression REG-28.

File src/graphindex_3.py.
class GraphIndex1: the class owns field `adjacency_list_15` and exposes `insert_node` and `expand_neighbors`. The method `insert_node` receives request_id=1066, layer_id=10, head_id=2, and token_span=(1056, 1119). The method `expand_neighbors` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_1, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0066: a stale `adjacency_list_15` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_graphindex_insert_node_4` references earlier bug BUG-0054 and later regression REG-29.

File src/queryplanner_4.py.
class QueryPlanner2: the class owns field `plan_cache_16` and exposes `estimate_cost` and `choose_candidate_set`. The method `estimate_cost` receives request_id=1067, layer_id=11, head_id=3, and token_span=(1072, 1135). The method `choose_candidate_set` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_2, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0067: a stale `plan_cache_16` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_queryplanner_estimate_cost_5` references earlier bug BUG-0055 and later regression REG-30.

File src/kvreader_5.py.
class KVReader3: the class owns field `block_handle_0` and exposes `read_key_block` and `read_value_block`. The method `read_key_block` receives request_id=1068, layer_id=12, head_id=4, and token_span=(1088, 1151). The method `read_value_block` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_3, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0068: a stale `block_handle_0` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_kvreader_read_key_block_6` references earlier bug BUG-0056 and later regression REG-31.

File src/tokenizerbridge_6.py.
class TokenizerBridge4: the class owns field `offset_table_1` and exposes `map_token_span` and `decode_piece`. The method `map_token_span` receives request_id=1069, layer_id=13, head_id=5, and token_span=(1104, 1167). The method `decode_piece` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_4, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0069: a stale `offset_table_1` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_tokenizerbridge_map_token_span_7` references earlier bug BUG-0057 and later regression REG-32.

File src/cachepage_7.py.
class CachePage0: the class owns field `page_table_2` and exposes `pin_page` and `evict_cold_page`. The method `pin_page` receives request_id=1070, layer_id=14, head_id=6, and token_span=(1120, 1183). The method `evict_cold_page` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_5, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0070: a stale `page_table_2` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_cachepage_pin_page_8` references earlier bug BUG-0058 and later regression REG-33.

File src/graphindex_8.py.
class GraphIndex1: the class owns field `adjacency_list_3` and exposes `insert_node` and `expand_neighbors`. The method `insert_node` receives request_id=1071, layer_id=15, head_id=7, and token_span=(1136, 1199). The method `expand_neighbors` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_6, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0071: a stale `adjacency_list_3` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_graphindex_insert_node_9` references earlier bug BUG-0059 and later regression REG-34.

File src/queryplanner_0.py.
class QueryPlanner2: the class owns field `plan_cache_4` and exposes `estimate_cost` and `choose_candidate_set`. The method `estimate_cost` receives request_id=1072, layer_id=16, head_id=0, and token_span=(1152, 1215). The method `choose_candidate_set` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_7, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0072: a stale `plan_cache_4` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_queryplanner_estimate_cost_10` references earlier bug BUG-0060 and later regression REG-35.

File src/kvreader_1.py.
class KVReader3: the class owns field `block_handle_5` and exposes `read_key_block` and `read_value_block`. The method `read_key_block` receives request_id=1073, layer_id=17, head_id=1, and token_span=(1168, 1231). The method `read_value_block` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_8, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0073: a stale `block_handle_5` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_kvreader_read_key_block_11` references earlier bug BUG-0061 and later regression REG-36.

File src/tokenizerbridge_2.py.
class TokenizerBridge4: the class owns field `offset_table_6` and exposes `map_token_span` and `decode_piece`. The method `map_token_span` receives request_id=1074, layer_id=18, head_id=2, and token_span=(1184, 1247). The method `decode_piece` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_9, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0074: a stale `offset_table_6` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_tokenizerbridge_map_token_span_12` references earlier bug BUG-0062 and later regression REG-0.

File src/cachepage_3.py.
class CachePage0: the class owns field `page_table_7` and exposes `pin_page` and `evict_cold_page`. The method `pin_page` receives request_id=1075, layer_id=19, head_id=3, and token_span=(1200, 1263). The method `evict_cold_page` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_10, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0075: a stale `page_table_7` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_cachepage_pin_page_13` references earlier bug BUG-0063 and later regression REG-1.

File src/graphindex_4.py.
class GraphIndex1: the class owns field `adjacency_list_8` and exposes `insert_node` and `expand_neighbors`. The method `insert_node` receives request_id=1076, layer_id=20, head_id=4, and token_span=(1216, 1279). The method `expand_neighbors` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_11, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0076: a stale `adjacency_list_8` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_graphindex_insert_node_14` references earlier bug BUG-0064 and later regression REG-2.

File src/queryplanner_5.py.
class QueryPlanner2: the class owns field `plan_cache_9` and exposes `estimate_cost` and `choose_candidate_set`. The method `estimate_cost` receives request_id=1077, layer_id=21, head_id=5, and token_span=(1232, 1295). The method `choose_candidate_set` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_12, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0077: a stale `plan_cache_9` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_queryplanner_estimate_cost_15` references earlier bug BUG-0065 and later regression REG-3.

File src/kvreader_6.py.
class KVReader3: the class owns field `block_handle_10` and exposes `read_key_block` and `read_value_block`. The method `read_key_block` receives request_id=1078, layer_id=22, head_id=6, and token_span=(1248, 1311). The method `read_value_block` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_0, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0078: a stale `block_handle_10` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_kvreader_read_key_block_16` references earlier bug BUG-0066 and later regression REG-4.

File src/tokenizerbridge_7.py.
class TokenizerBridge4: the class owns field `offset_table_11` and exposes `map_token_span` and `decode_piece`. The method `map_token_span` receives request_id=1079, layer_id=23, head_id=7, and token_span=(1264, 1327). The method `decode_piece` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_1, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0079: a stale `offset_table_11` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_tokenizerbridge_map_token_span_17` references earlier bug BUG-0067 and later regression REG-5.

File src/cachepage_8.py.
class CachePage0: the class owns field `page_table_12` and exposes `pin_page` and `evict_cold_page`. The method `pin_page` receives request_id=1080, layer_id=24, head_id=0, and token_span=(1280, 1343). The method `evict_cold_page` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_2, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0080: a stale `page_table_12` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_cachepage_pin_page_18` references earlier bug BUG-0068 and later regression REG-6.

File src/graphindex_0.py.
class GraphIndex1: the class owns field `adjacency_list_13` and exposes `insert_node` and `expand_neighbors`. The method `insert_node` receives request_id=1081, layer_id=25, head_id=1, and token_span=(1296, 1359). The method `expand_neighbors` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_3, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0081: a stale `adjacency_list_13` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_graphindex_insert_node_19` references earlier bug BUG-0069 and later regression REG-7.

File src/queryplanner_1.py.
class QueryPlanner2: the class owns field `plan_cache_14` and exposes `estimate_cost` and `choose_candidate_set`. The method `estimate_cost` receives request_id=1082, layer_id=26, head_id=2, and token_span=(1312, 1375). The method `choose_candidate_set` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_4, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0082: a stale `plan_cache_14` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_queryplanner_estimate_cost_20` references earlier bug BUG-0070 and later regression REG-8.

File src/kvreader_2.py.
class KVReader3: the class owns field `block_handle_15` and exposes `read_key_block` and `read_value_block`. The method `read_key_block` receives request_id=1083, layer_id=27, head_id=3, and token_span=(1328, 1391). The method `read_value_block` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_5, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0083: a stale `block_handle_15` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_kvreader_read_key_block_21` references earlier bug BUG-0071 and later regression REG-9.

File src/tokenizerbridge_3.py.
class TokenizerBridge4: the class owns field `offset_table_16` and exposes `map_token_span` and `decode_piece`. The method `map_token_span` receives request_id=1084, layer_id=0, head_id=4, and token_span=(1344, 1407). The method `decode_piece` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_6, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0084: a stale `offset_table_16` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_tokenizerbridge_map_token_span_22` references earlier bug BUG-0072 and later regression REG-10.

File src/cachepage_4.py.
class CachePage0: the class owns field `page_table_0` and exposes `pin_page` and `evict_cold_page`. The method `pin_page` receives request_id=1085, layer_id=1, head_id=5, and token_span=(1360, 1423). The method `evict_cold_page` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_7, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0085: a stale `page_table_0` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_cachepage_pin_page_23` references earlier bug BUG-0073 and later regression REG-11.

File src/graphindex_5.py.
class GraphIndex1: the class owns field `adjacency_list_1` and exposes `insert_node` and `expand_neighbors`. The method `insert_node` receives request_id=1086, layer_id=2, head_id=6, and token_span=(1376, 1439). The method `expand_neighbors` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_8, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0086: a stale `adjacency_list_1` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_graphindex_insert_node_24` references earlier bug BUG-0074 and later regression REG-12.

File src/queryplanner_6.py.
class QueryPlanner2: the class owns field `plan_cache_2` and exposes `estimate_cost` and `choose_candidate_set`. The method `estimate_cost` receives request_id=1087, layer_id=3, head_id=7, and token_span=(1392, 1455). The method `choose_candidate_set` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_9, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0087: a stale `plan_cache_2` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_queryplanner_estimate_cost_25` references earlier bug BUG-0075 and later regression REG-13.

File src/kvreader_7.py.
class KVReader3: the class owns field `block_handle_3` and exposes `read_key_block` and `read_value_block`. The method `read_key_block` receives request_id=1088, layer_id=4, head_id=0, and token_span=(1408, 1471). The method `read_value_block` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_10, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0088: a stale `block_handle_3` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_kvreader_read_key_block_26` references earlier bug BUG-0076 and later regression REG-14.

File src/tokenizerbridge_8.py.
class TokenizerBridge4: the class owns field `offset_table_4` and exposes `map_token_span` and `decode_piece`. The method `map_token_span` receives request_id=1089, layer_id=5, head_id=1, and token_span=(1424, 1487). The method `decode_piece` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_11, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0089: a stale `offset_table_4` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_tokenizerbridge_map_token_span_27` references earlier bug BUG-0077 and later regression REG-15.

File src/cachepage_0.py.
class CachePage0: the class owns field `page_table_5` and exposes `pin_page` and `evict_cold_page`. The method `pin_page` receives request_id=1090, layer_id=6, head_id=2, and token_span=(1440, 1503). The method `evict_cold_page` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_12, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0090: a stale `page_table_5` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_cachepage_pin_page_28` references earlier bug BUG-0078 and later regression REG-16.

File src/graphindex_1.py.
class GraphIndex1: the class owns field `adjacency_list_6` and exposes `insert_node` and `expand_neighbors`. The method `insert_node` receives request_id=1091, layer_id=7, head_id=3, and token_span=(1456, 1519). The method `expand_neighbors` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_0, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0091: a stale `adjacency_list_6` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_graphindex_insert_node_29` references earlier bug BUG-0079 and later regression REG-17.

File src/queryplanner_2.py.
class QueryPlanner2: the class owns field `plan_cache_7` and exposes `estimate_cost` and `choose_candidate_set`. The method `estimate_cost` receives request_id=1092, layer_id=8, head_id=4, and token_span=(1472, 1535). The method `choose_candidate_set` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_1, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0092: a stale `plan_cache_7` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_queryplanner_estimate_cost_30` references earlier bug BUG-0080 and later regression REG-18.

File src/kvreader_3.py.
class KVReader3: the class owns field `block_handle_8` and exposes `read_key_block` and `read_value_block`. The method `read_key_block` receives request_id=1093, layer_id=9, head_id=5, and token_span=(1488, 1551). The method `read_value_block` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_2, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0093: a stale `block_handle_8` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_kvreader_read_key_block_0` references earlier bug BUG-0081 and later regression REG-19.

File src/tokenizerbridge_4.py.
class TokenizerBridge4: the class owns field `offset_table_9` and exposes `map_token_span` and `decode_piece`. The method `map_token_span` receives request_id=1094, layer_id=10, head_id=6, and token_span=(1504, 1567). The method `decode_piece` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_3, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0094: a stale `offset_table_9` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_tokenizerbridge_map_token_span_1` references earlier bug BUG-0082 and later regression REG-20.

File src/cachepage_5.py.
class CachePage0: the class owns field `page_table_10` and exposes `pin_page` and `evict_cold_page`. The method `pin_page` receives request_id=1095, layer_id=11, head_id=7, and token_span=(1520, 1583). The method `evict_cold_page` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_4, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0095: a stale `page_table_10` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_cachepage_pin_page_2` references earlier bug BUG-0083 and later regression REG-21.

File src/graphindex_6.py.
class GraphIndex1: the class owns field `adjacency_list_11` and exposes `insert_node` and `expand_neighbors`. The method `insert_node` receives request_id=1096, layer_id=12, head_id=0, and token_span=(1536, 1599). The method `expand_neighbors` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_5, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0096: a stale `adjacency_list_11` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_graphindex_insert_node_3` references earlier bug BUG-0084 and later regression REG-22.

File src/queryplanner_7.py.
class QueryPlanner2: the class owns field `plan_cache_12` and exposes `estimate_cost` and `choose_candidate_set`. The method `estimate_cost` receives request_id=1097, layer_id=13, head_id=1, and token_span=(1552, 1615). The method `choose_candidate_set` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_6, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0097: a stale `plan_cache_12` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_queryplanner_estimate_cost_4` references earlier bug BUG-0085 and later regression REG-23.

File src/kvreader_8.py.
class KVReader3: the class owns field `block_handle_13` and exposes `read_key_block` and `read_value_block`. The method `read_key_block` receives request_id=1098, layer_id=14, head_id=2, and token_span=(1568, 1631). The method `read_value_block` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_7, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0098: a stale `block_handle_13` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_kvreader_read_key_block_5` references earlier bug BUG-0086 and later regression REG-24.

File src/tokenizerbridge_0.py.
class TokenizerBridge4: the class owns field `offset_table_14` and exposes `map_token_span` and `decode_piece`. The method `map_token_span` receives request_id=1099, layer_id=15, head_id=3, and token_span=(1584, 1647). The method `decode_piece` must not drop exact value payloads when the selected key index is only approximate. Pseudo-code: if score_margin < threshold_8, call fallback_exact_read(block_handle); otherwise gather candidate_values from the selected block list. Bug note BUG-0099: a stale `offset_table_14` entry caused a query to reuse an old graph neighborhood after the cache was compacted. Design note: keys are allowed to be compressed for routing, but values remain high-fidelity until the final read path. The unit test `test_tokenizerbridge_map_token_span_6` references earlier bug BUG-0087 and later regression REG-25.
