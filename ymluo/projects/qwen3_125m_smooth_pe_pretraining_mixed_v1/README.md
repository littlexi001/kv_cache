# Natural-text PE pretraining patch

This directory is a deployment bundle for the independent
`natural95_synth5_v1` experiment in
`/home/fdong/ymluo/projects/qwen3_125m_smooth_pe_pretraining`.

It deliberately does not overwrite the completed synthetic-only runs.

