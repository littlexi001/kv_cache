#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_DIR="$(cd "${SCRIPT_DIR}/.." && pwd)"

RUN_NAME_VALUE="${RUN_NAME:-structured-big-test-attn-cluster}"
export CKPT_FILE="${CKPT_FILE:-${PROJECT_DIR}/outputs/train/${RUN_NAME_VALUE}/checkpoints/10000.pth}"

export SEQ_LEN="${SEQ_LEN:-512}"
export DEBUG_VOCAB_SIZE="${DEBUG_VOCAB_SIZE:-1024}"
export DEBUG_MAX_POSITION_EMBEDDINGS="${DEBUG_MAX_POSITION_EMBEDDINGS:-1024}"
export STRUCTURED_TOPIC_COUNT="${STRUCTURED_TOPIC_COUNT:-32}"
export STRUCTURED_ENTITIES_PER_TOPIC="${STRUCTURED_ENTITIES_PER_TOPIC:-16}"
export STRUCTURED_SHARED_ENTITY_COUNT="${STRUCTURED_SHARED_ENTITY_COUNT:-128}"
export STRUCTURED_VERB_COUNT="${STRUCTURED_VERB_COUNT:-32}"
export STRUCTURED_FUNCTION_TOKEN_COUNT="${STRUCTURED_FUNCTION_TOKEN_COUNT:-32}"
export STRUCTURED_NOISE_TOKEN_COUNT="${STRUCTURED_NOISE_TOKEN_COUNT:-128}"
export STRUCTURED_NOISE_RATE="${STRUCTURED_NOISE_RATE:-0.5}"
export STRUCTURED_AMBIGUITY_RATE="${STRUCTURED_AMBIGUITY_RATE:-0.7}"
export STRUCTURED_COPY_RATE="${STRUCTURED_COPY_RATE:-0.4}"
export STRUCTURED_BRIDGE_RATE="${STRUCTURED_BRIDGE_RATE:-0.45}"
export STRUCTURED_MIN_SPAN_UNITS="${STRUCTURED_MIN_SPAN_UNITS:-2}"
export STRUCTURED_MAX_SPAN_UNITS="${STRUCTURED_MAX_SPAN_UNITS:-10}"

exec bash "${SCRIPT_DIR}/run_eval.sh" "$@"
