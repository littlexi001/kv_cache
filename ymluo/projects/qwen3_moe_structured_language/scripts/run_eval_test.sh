#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_DIR="$(cd "${SCRIPT_DIR}/.." && pwd)"

RUN_NAME_VALUE="${RUN_NAME:-structured-test-attn-cluster}"
export CKPT_FILE="${CKPT_FILE:-${PROJECT_DIR}/outputs/train/${RUN_NAME_VALUE}/checkpoints/10000.pth}"

export SEQ_LEN="${SEQ_LEN:-256}"
export DEBUG_VOCAB_SIZE="${DEBUG_VOCAB_SIZE:-192}"
export DEBUG_MAX_POSITION_EMBEDDINGS="${DEBUG_MAX_POSITION_EMBEDDINGS:-512}"
export STRUCTURED_TOPIC_COUNT="${STRUCTURED_TOPIC_COUNT:-8}"
export STRUCTURED_ENTITIES_PER_TOPIC="${STRUCTURED_ENTITIES_PER_TOPIC:-8}"
export STRUCTURED_SHARED_ENTITY_COUNT="${STRUCTURED_SHARED_ENTITY_COUNT:-16}"
export STRUCTURED_VERB_COUNT="${STRUCTURED_VERB_COUNT:-12}"
export STRUCTURED_FUNCTION_TOKEN_COUNT="${STRUCTURED_FUNCTION_TOKEN_COUNT:-12}"
export STRUCTURED_NOISE_TOKEN_COUNT="${STRUCTURED_NOISE_TOKEN_COUNT:-32}"
export STRUCTURED_NOISE_RATE="${STRUCTURED_NOISE_RATE:-0.25}"
export STRUCTURED_AMBIGUITY_RATE="${STRUCTURED_AMBIGUITY_RATE:-0.35}"
export STRUCTURED_COPY_RATE="${STRUCTURED_COPY_RATE:-0.25}"
export STRUCTURED_BRIDGE_RATE="${STRUCTURED_BRIDGE_RATE:-0.25}"

exec bash "${SCRIPT_DIR}/run_eval.sh" "$@"
